{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "portra-operator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "portra-operator.resourceName" (dict "suffix" "app-viewer-role" "context" $) }}
rules:
    - apiGroups:
        - core.portra.run
      resources:
        - apps
      verbs:
        - get
        - list
        - watch
    - apiGroups:
        - core.portra.run
      resources:
        - apps/status
      verbs:
        - get
{{- end }}
